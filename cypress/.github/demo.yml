name: PIPELINE DEMO
on: push
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: PIPELINE RUNNER
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6